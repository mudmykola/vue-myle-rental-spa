import { createStore } from "vuex";
import services from "./modules/services";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    services,
  },
});
